source 'http://rubygems.org'

gem 'rails', '3.0.1'
gem 'mysql'               # postgres
gem 'foreigner'        # foreign key constraints
gem 'omniauth'         # authentication
gem 'haml', '>= 3.0.0' # markup and css templates
gem 'haml-rails'       # use haml as templating engine
gem 'jammit'           # asset packaging
gem 'compass'          # sass mixins and generators

gem 'ruby-debug', :group => [:development, :test]
gem 'rspec-rails', '>= 2.0.1', :group => [:development, :test]
gem 'cucumber-rails', :group => :test
gem 'capybara', :group => :test
